{% set color = "osg-tag--" ~ tag.color | default('success') %}
{% set tagVariant = tag.variant | default('osg-v-default') %}
{% set tagModifiers = tag.modifiers | default(null) %}

<a href="{{ tag.url }}" class="osg-tag {{ tagVariant }} {{ color }} {{ tagModifiers }}">
  {{ tag.content }}
</a>
