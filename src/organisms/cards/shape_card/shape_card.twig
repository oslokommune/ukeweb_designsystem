{% set hasStateText = shapeCard.stateText is defined and shapeCard.stateText is not empty %}
{% set hasTags = tags is defined and tags is not empty %}
{% set hasSearchResult = shapeCard.searchResult is defined and shapeCard.searchResult is not empty %}

<div class="osg-shapeCard {{ shapeCard.variant | default('osg-v-default')  }} {{ shapeCard.modifiers | default(null) }}">
  <a class="osg-wrapper" href="{{ shapeCard.url }}" title="{{ figure.img.alt }}">
    <div class="osg-image-container" href="{{ shapeCard.url }}" aria-hidden="true">
      {% include "atoms/decorators/figure/figure.twig" %}
      {% if hasStateText %}
      <div class="osg-state-text">{{ shapeCard.stateText }}</div>
      {% endif %}
    </div>
  </a>
  <div class="osg-box-shape-wrapper">
    {% embed 'molecules/content_display/content_box/content_box.twig' %}
      {% block head %}
        <a class="osg-wrapper" href="{{ shapeCard.url }}" aria-hidden="true">
          {{ parent() }}
        </a>
      {% endblock %}
      {% block body %}
        <a class="osg-wrapper" href="{{ shapeCard.url }}" aria-hidden="true">
          {{ parent() }}
        </a>
      {% endblock %}
      {% block footer %}
        {{ parent() }}
        {% if hasTags %}
          {% include 'molecules/navigation/tags/tags.twig' %}
        {% endif %}
        {% if hasSearchResult %}
          <a href="{{ shapeCard.url }}" class="osg-search-result">
            {{ shapeCard.searchResult | default(null) }}
          </a>
        {% endif %}
      {% endblock %}
    {% endembed %}
    {% include 'atoms/decorators/shape/shape.twig' %}
  </div>
</div>
