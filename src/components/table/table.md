Design rule: `.osg-table--left-right` is only to be used on two column tables.

You **must** use `<thead>` and `<tbody>` to structure headings and table content.

`<tfoot>` and `<colgroup>` / `<col>` is not supported/designed.
