##### Use
The **osg-button** class can be used on:
* `<a>` anchor links
* `<button>` form buttons
* `<input type="submit">` submit inputs
* `<input type="reset">` reset inputs

For buttons that are active you should use the active state `osg-button--active`.
